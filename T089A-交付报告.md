# T089A 交付报告

## 改动摘要

- 扩展 `SessionMeta` v2：新增可选 `origin` 与 `sourceInstance`，旧 meta
  不迁移也可原样读取。
- 在 `~/.claude-session-manager/app-config.json` 持久化安装级
  `deviceId`；新建 Library meta 首次写入来源，已有来源永不被同步覆盖。
- Library-only 会话优先用 `origin.deviceId` 判定跨机，修复同用户名异机
  无法识别；旧 meta 保持原用户名路径猜测行为。
- 新增纯函数 `planSessionRecovery()`：输出物理 ID、目标实例/路径、导入
  路由、冲突预判和 iCloud 待物化清单；已知远端和非标准来源必须显式
  选择目标。
- 新增四类完全合成、显式脱敏 fixture：正常、逻辑/物理双 ID、真实坏
  JSONL 行、iCloud placeholder 元数据。

## AC 自核

- [x] 旧格式 `.swob-session.json` 无新字段时正常读入，不伪造默认来源；
  保持现有 resume/library-only 行为。
- [x] 新 meta 含 `origin`/`sourceInstance`；deviceId 存储位置、生成与写入
  时机、首次来源不可覆盖规则已写入代码注释与
  `docs/recovery-domain-model.md`。
- [x] planner 覆盖并测试：目标同名文件冲突、逻辑 ID 不等于物理 ID、
  非标准目录拒绝/显式导入、标准实例、现存/消失 Window 实例、同用户名
  跨机来源。
- [x] planner 零副作用：模块不导入 fs；测试 spy 断言不调用写入、建目录、
  复制、改名、删除及路径探测 API。
- [x] `npm test`：34 个测试文件、389/389 tests 通过。
- [x] `npx electron-vite build`：通过；仅输出既有 `::highlight` CSS 优化
  警告。
- [x] fixture 无真实 transcript、主机、用户名、路径、密钥或凭据；标识
  使用合成 UUID 或 `xx…末4位` 形态。

## 边界自核

- [x] 未新增任何实际恢复、发布、复制或删除逻辑；旧恢复调用链未切换。
- [x] 未读取或写入真实 `~/.claude/projects`；测试只使用临时 HOME 和
  `/fixture/...` 合成路径。
- [x] 未改 renderer、依赖声明、lockfile、`.git` 或任何
  `transcript-origin` 相关文件。
- [x] 未 commit、未 push。
- [x] 使用 `apply_patch` 落盘；`git diff --check` 通过，`git diff` 非空，
  `git status --short` 同时确认新增文件均在允许范围。

## 补充检查

`npx tsc --noEmit -p tsconfig.node.json` 仍报告两处既有
`src/main/auto-updater.ts` 的 `releaseNotes` nullability 类型错误。本次改动
文件没有新增 TypeScript 诊断；该项不属于本单 AC，未越界修改。
