# T088R2 交付报告

## 逐条修复对照

| 问题 | 改动 | 对应测试 |
| --- | --- | --- |
| P1-1 首个有效文本错误 | `firstContentText()` 跳过空白字符串和空白 `text` part，再对首个非空白文本做标签判定。 | `P1-1 回归：跳过空白 text 后识别首个有效 task-notification 标签`，覆盖报告原反例。 |
| P1-2 promptSource 错误回退 human | human 改为白名单证据：仅 `origin.kind:"human"` 或 `typed` / `queued` / `suggestion_accepted` 且普通 content 可判 human；`sdk`、未知/未来值、缺失字段均不兜底。已知机器标签、meta、system 和占位规则先排除。 | `P1-2 回归：sdk、未来值与缺失 promptSource 均不回退 human`；真人白名单三值也有正例。 |
| P1-3 缓存丢来源字段且旧缓存未失效 | `compactLineageMessage()` 保留 `origin` / `promptSource` / `isMeta`，同时保留顶层工具来源证据；`CACHE_VERSION` 从 19 升到 20。 | `P1-3：压缩缓存保留来源证据，多文件 summary 重建不丢判定字段`；`P1-3：版本 19 的旧缓存强制失效，不污染来源判定`。后者向旧缓存注入错误 human 摘要并验证重新解析。 |
| P2-1 图片占位回归 | 纯 `[Image: source: …]`（含连续多个占位）判 `unknown`。理由：该形态能排除真人发言，但不能独立证明具体 tool 来源。 | `P2-1【曾经的 bug】：纯图片来源占位字符串不是 human`。 |
| P2-2 顶层工具字段漏判 | `sourceToolAssistantUUID` / `toolUseResult` 加入类型与判定树，优先于 `promptSource`；压缩缓存保留其判定证据。 | `P2-2：顶层工具字段优先于普通 text 与真人 promptSource`，两个字段分别覆盖。 |
| P2-3 证据措辞不诚实 | 集成用例由“真实 Claude 结构会话”改名为“合成结构样本”；映射文档继续明确记录本机无独立 hook 正例，不伪造样本。 | `合成结构样本：机器注入带来源标头，真人消息保持原样`。 |

补充闭环：严格白名单暴露了 resume audit 把 Codex/Cursor/OpenCode/Zcode 的用户消息再次套用 Claude-only 判定器的问题。现按来源分流：Claude 使用严格 origin 判定，其它来源采用各自 loader 的 `isSystemGenerated` 结果并排除 `tool_result`，原有五来源 L3 测试保持全绿。

## AC 自核

- [x] 6 项修复均有对应测试；P1-1 覆盖交叉验收报告给出的“首元素空白、次元素 task-notification”原输入。
- [x] P1-2 白名单制完成：无法判定一律 `unknown`，不存在普通 content → human 的最终兜底。
- [x] P1-3 缓存版本 19 → 20；旧版本污染用例通过；多文件压缩重建字段保留用例通过。
- [x] P2-1 图片占位负例、P2-2 两个顶层工具字段均有回归覆盖。
- [x] `npm test`：34 个测试文件、388/388 tests 通过。
- [x] `npx electron-vite build`：通过。构建仅报告 renderer 既有 `::highlight` CSS 优化警告，本单未修改 renderer。
- [x] `docs/transcript-origin-mapping.md` 已同步 promptSource 白名单、首个非空白文本、图片占位归 `unknown` 的理由、顶层工具字段优先级及 hook 真实样本缺口。
- [x] `git diff --check` 通过；`git diff` 非空。
- [x] 改动只在工作区根交付报告、`docs/transcript-origin-mapping.md` 与 `src/main/**`；未改 renderer、依赖、`package.json`、lockfile或 `.git`。
- [x] 未 commit、未 push。

## 未过项

无。
