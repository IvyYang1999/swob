appId: com.swob.app
productName: Swob
directories:
  buildResources: build
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.*'
  - '!{tsconfig,tsconfig.*}.json'
mac:
  target:
    - target: dmg
      arch:
        - arm64
        - x64
  artifactName: swob-${version}-${arch}.${ext}
